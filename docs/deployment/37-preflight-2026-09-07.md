# #37 read-only production preflight — 2026-09-07

## Verdict

`BLOCKED`

Canonical desired Git SHA:
`0d68aebc3d83cc528398ca0e3348b7306db6ffe4`.

This preflight was observation and deployment planning only. No production
file, OpenClaw job/config, service, scheduler/model, credential, ledger,
Zernio, Telegram, approval or publication state was changed.

## Component completion versus integration readiness

#27–#36 remain CLOSED/COMPLETED and valid as component-level repository
engineering. The preflight found previously uncaptured integration
prerequisites between those components and the live scheduler/runtime.
Deployable end-to-end M0 integration is therefore incomplete; this does not
reopen or weaken any accepted #27–#36 result.

## Durable production facts

- Installed OpenClaw version: `2026.8.2`.
- Gateway: healthy at host level.
- Scheduler: 10 jobs enabled.
- Morning Editorial and Daily Analytics: legacy job payloads still active.
- Story: no Story-specific live job.
- Scheduler-native `failureAlert`: supported, not configured for Morning or
  Daily.
- Telegram owner target: `PRESENT/READABLE`; value intentionally omitted.
- Zernio host-level OAuth/MCP: healthy.
- Zernio scheduled-session bootstrap: errors still observed.
- Dedicated #29 `ZERNIO_ANALYTICS_API_TOKEN`: absent.
- Cadence state: production publish ledger contains two historical valid JSON
  rows without the newer required `format` field and is incompatible with the
  strict #32 adapter.
- Unresolved publication state: one Astra `UNKNOWN` and two
  `CHECK_REQUIRED` records remain operator decisions, not automatic
  deployment actions.

Scheduler success was not treated as domain success. Host-level Zernio health
was not treated as proof that the scheduled-session draft bridge works.

## Confirmed blockers

1. No reviewed scheduler-facing binding exists for exact occurrence identity
   → Morning/Daily runtime → persisted #27 outcome → #30 domain notifier.
2. No reviewed backward-compatible or reversible migration path exists for
   historical publish-ledger rows lacking `format`.
3. No reviewed cadence/state → `PREPARE_STORY` → #33 Story production runner
   exists.
4. Story/main pipelines have injected Telegram preview protocols but no
   production sender implementation.
5. No reviewed Radar candidate → #35 identity → #36 router/dispatcher →
   Story/main production runner exists.
6. No reviewed secure runtime injection mechanism exists for
   `ZERNIO_ANALYTICS_API_TOKEN`.
7. Scheduled-session Zernio bootstrap remains unproven for the MCP-backed
   Story/main draft bridge.
8. Exact safe cron/job payloads cannot be authored until orchestration and
   occurrence-ID binding exist.

The production publisher wrapper also needs the already-reviewed
Story-supersession version during eventual deployment. That remains a known
file deployment delta, not a new engineering-design issue.

## Dependency issues

- #65 — Define NullOne Application Runtime and replaceable infrastructure
  adapters.
- #59 — Add NullOne scheduled workflow orchestration for Morning and Daily.
- #60 — Make cadence state reads compatible with historical publish-ledger
  rows.
- #61 — Define secure scheduled injection for
  `ZERNIO_ANALYTICS_API_TOKEN`.
- #62 — Implement NullOne `StoryWorkflow` and shared review-delivery adapter.
- #63 — Implement NullOne `BreakingWorkflow` orchestration.

Dependency order:

- #65 and #60 can begin independently.
- #59 and #61 depend on #65.
- #62 depends on #65 + #60.
- #63 depends on #65 + #62 and indirectly on #60.
- #37 depends on #65 and all five integration dependencies.

#65 formalizes an existing OpenClaw-independence intent that #27–#36 already
partly reflected through deterministic cores and injected dependencies. The
missing contract was the first-class NullOne Application Runtime that owns
workflow composition. OpenClaw remains the current trigger/scheduler adapter,
not the owner of Morning, Analytics, Story, or Breaking semantics.

No separate generic scheduled-session Zernio issue was created. The direct
#29 GET-only adapter makes scheduled-session MCP unnecessary for Daily
Analytics; #61 supplies its secure credential path. Story and breaking still
use the MCP-backed review-draft bridge, so #62 must prove that scheduled path
or deliver a reviewed equivalent connector path, and #63 inherits it.

## Production boundary

Deployment remains prohibited until #65 and #59–#63 are accepted/merged
through normal review and a repeated strictly read-only #37 preflight returns
`READY_FOR_CONTROLLED_DEPLOYMENT`. No synthetic production cycle is allowed.
Natural live behavior is observed only after a separately authorized
controlled deployment under #37.
